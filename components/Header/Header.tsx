import { HeaderContainer, HeaderTitle, Byline } from "./Header.styles";

const Header = () => (
  <HeaderContainer>
    <HeaderTitle>THE BROKEN BANANA</HeaderTitle>
    <Byline>An online magazine for troubled fruit</Byline>
  </HeaderContainer>
);

export default Header;
